language: php
cache:
  directories:
    - $HOME/.composer/cache
before_script:
  - composer install
script:
  # Replace this build step with your own build step if using travis
  - ./vendor/bin/concrete5 c5:status
